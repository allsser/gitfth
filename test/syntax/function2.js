console.log(Math.round(1.6)); //round 함수는 반올림을 해주는 함수이다.
console.log(Math.round(1.4));

function sum(first, second){ //parameter 파라메터
  console.log('a');
  return first + second; //return이 나오면 여기서 끝이난다.
  console.log('b');
}

console.log(sum(2,4)); //argument 인자
